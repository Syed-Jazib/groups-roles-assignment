import { AccessRightsFilterPipe } from './access-rights-filter.pipe';

describe('AccessRightsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AccessRightsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
